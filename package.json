{
  "name": "90plus",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd api && pnpm run start\" \"cd web && pnpm run dev\" ",
    "build": "concurrently \"cd api && pnpm run build\" \"cd web && pnpm run build\"",
    "start:prod": "pnpm --filter api start:prod",
    "build:web": "cd web && pnpm build",
    "start:api": "pnpm --filter api start:prod",
    "start": "pnpm build:web && pnpm start:api",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
